## 1. 修改分布线计算逻辑
- [x] 1.1 移除 `BRACKET_GAP` 常量，改用动态居中计算
- [x] 1.2 多子女场景：计算所有子代中最高（最小 Y）的 `childTopY = min(child.y) - nodeRadius`，分布线 `midY = (startY + childTopY) / 2`
- [x] 1.3 单子女场景：同样使用居中公式 `midY = (startY + childTopY) / 2`
- [x] 1.4 添加安全下限：当 `midY <= startY` 时（父子距离极近），回退为 `startY + 10` 避免反转

## 2. 验证
- [ ] 2.1 手动验证：夫妻 + 多子女场景，分布横线位于父母底部和子代顶部之间的居中位置，上下竖线长度近似相等
- [ ] 2.2 手动验证：单亲 + 单子女场景，竖线从父母底部均匀连接到子代顶部
- [ ] 2.3 手动验证：多代家庭场景，各层 bracket 线比例一致
